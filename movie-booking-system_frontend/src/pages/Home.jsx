import React from "react";
import { Typography } from "@mui/material";

const Home = () => (
  <div align="center" style={{ marginTop: "20px", flex: "1", display: "flex", flexDirection: "column", justifyContent: "center", alignItems: "center" }}>
    <Typography variant="h4">Welcome to Movie Booking System 🎬</Typography>
  </div>
);

export default Home;
